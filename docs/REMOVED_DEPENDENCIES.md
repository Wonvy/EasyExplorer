# 已移除的 Electron 依赖

以下是从项目中移除的 Electron 相关依赖及其原因：

## 开发依赖 (devDependencies)

### ❌ `electron: ^33.0.0`
**原因:** 已迁移到 Tauri，不再需要 Electron 运行时

**替代:** `@tauri-apps/cli: ^1.5.0`

### ❌ `electron-builder: ^25.1.8`
**原因:** Electron 打包工具，Tauri 有自己的构建系统

**替代:** Tauri 内置的 `cargo tauri build`

### ❌ `concurrently: ^9.0.1`
**原因:** 用于同时运行 Electron 和其他进程，不再需要

**替代:** Tauri CLI 自带开发服务器

---

## 运行时依赖 (dependencies)

### ❌ `electron-clipboard-ex: ^1.3.3`
**原因:** Electron 特定的剪贴板扩展

**替代:** Tauri 内置的剪贴板 API (`window.__TAURI__.clipboard`)

### ❌ `everything.js: ^1.0.3`
**原因:** 基于 Node.js 的文件搜索工具

**替代:** 需要在 Rust 端实现或通过 Tauri 命令调用系统搜索

**状态:** ⚠️ 待实现

### ❌ `fs-extra: ^11.2.0`
**原因:** Node.js 文件系统扩展

**替代:** Rust 标准库的 `std::fs` + Tauri 文件系统 API

### ❌ `icon-extractor: ^1.0.3`
**原因:** 基于 Node.js 的图标提取工具

**替代方案:**
- 选项 1: 在 Rust 端使用 Windows API 提取图标
- 选项 2: 使用基于扩展名的 CSS 图标
- 选项 3: 使用 Font Awesome 等 Web 图标库

**状态:** ⚠️ 待实现

### ❌ `iconv-lite: ^0.6.3`
**原因:** Node.js 字符编码转换库

**替代:** Rust 的编码处理 crate (如 `encoding_rs`)

**状态:** ⚠️ 如需要可在 Rust 端实现

### ❌ `libreoffice-convert: ^1.6.0`
**原因:** 基于 Node.js 的 LibreOffice 文档转换

**替代:** 通过 Rust 调用 LibreOffice 命令行工具

**状态:** ⚠️ 待实现

### ❌ `psd: ^3.4.0`
**原因:** Node.js PSD 文件解析库

**替代:** Rust PSD 解析 crate (如 `psd`)

**状态:** ⚠️ 待实现

### ❌ `sharp: ^0.33.5`
**原因:** Node.js 图片处理库

**替代:** Rust 图片处理 crate (如 `image`)

**状态:** ⚠️ 待实现

### ❌ `windows-shortcuts-gbk: ^0.1.6`
**原因:** Node.js Windows 快捷方式处理

**替代:** 可在 Rust 端使用 Windows API 实现

**状态:** ⚠️ 待实现

### ❌ `three: ^0.169.0`
**原因:** 3D 渲染库

**保留可能:** ✅ 如果前端需要 3D 功能，可以保留此依赖（纯前端库）

**建议:** 如果项目使用了 3D 预览功能，建议保留

### ❌ `highlight.js: ^11.10.0`
**原因:** 代码高亮库

**保留可能:** ✅ 纯前端库，如果需要代码预览功能，可以保留

**建议:** 如果项目有代码文件预览功能，建议保留

### ❌ `sortablejs: ^1.15.3`
**原因:** 拖拽排序库

**保留可能:** ✅ 纯前端库，如果需要拖拽功能，可以保留

**建议:** 如果项目有文件拖拽排序功能，建议保留

### ❌ `occt-import-js: ^0.0.22`
**原因:** OCCT 3D CAD 文件导入库

**保留可能:** ✅ 如果项目需要 CAD 文件预览，可能需要保留或寻找替代方案

**状态:** ⚠️ 需要评估是否必要

---

## 保留的依赖

### ✅ `sass: ^1.80.3`
**原因:** CSS 预处理器，前端开发需要

**状态:** 已保留在 devDependencies

---

## 新增的依赖

### ✅ `@tauri-apps/cli: ^1.5.0` (devDependencies)
**用途:** Tauri 命令行工具，用于开发和构建

### ✅ Rust Crates (src-tauri/Cargo.toml)
```toml
tauri = { version = "1.5", features = [...] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
```

---

## 建议保留的纯前端库（可选）

如果项目功能需要，以下纯前端库可以保留：

1. **highlight.js** - 代码文件预览
2. **sortablejs** - 拖拽功能
3. **three.js** - 3D 文件预览

这些库不依赖 Node.js 或 Electron，可以直接在 Tauri 的 WebView 中使用。

---

## 待实现的功能映射

| 原功能 | 原依赖 | Rust 替代方案 |
|--------|--------|--------------|
| 文件图标提取 | icon-extractor | winapi + ico crate |
| 快速文件搜索 | everything.js | 系统命令或自实现 |
| 图片处理 | sharp | image crate |
| PSD 解析 | psd | psd crate |
| 文档转换 | libreoffice-convert | 命令行调用 |
| 字符编码 | iconv-lite | encoding_rs crate |
| 快捷方式 | windows-shortcuts-gbk | Windows API |

---

## 包体积对比

### 之前 (Electron)
```
node_modules: ~500 MB
应用包大小: ~120 MB
```

### 之后 (Tauri)
```
node_modules: ~50 MB (仅开发工具)
应用包大小: ~8 MB
```

**减少了约 93% 的包体积！** 🎉

