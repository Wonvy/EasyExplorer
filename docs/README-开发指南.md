# EasyExplorer 开发指南

## 🚀 快速开始

### 一键启动（推荐）⭐

```bash
npm run tauri dev
```

**就这么简单！** 这会自动：
- ✅ 启动 HTTP 服务器
- ✅ 启动 SCSS 编译监听
- ✅ 启动 Tauri 应用
- ✅ 启用热重载

---

## 📋 常用命令

### 开发模式

```bash
# 方式 1: 使用 Tauri CLI（推荐）
npm run tauri dev

# 方式 2: 使用 concurrently
npm run dev

# 单独运行 HTTP 服务器
npm run serve

# 单独运行 SCSS 监听
npm run sass
```

### 构建生产版本

```bash
# 构建应用（自动编译 SCSS）
npm run build

# 只编译 SCSS
npm run sass:build
```

---

## 🔥 热重载功能

### 前端热重载
修改以下文件会自动刷新浏览器：
- ✅ HTML 文件（`src/*.html`）
- ✅ JavaScript 文件（`src/*.js`）
- ✅ CSS 文件（`src/css/*.css`）
- ✅ SCSS/SASS 文件（`src/scss/*.sass` → 自动编译）

### 后端热重载
修改以下文件会自动重新编译并重启应用：
- ✅ Rust 文件（`src-tauri/src/*.rs`）
- ✅ Cargo 配置（`src-tauri/Cargo.toml`）

---

## 📁 项目结构

```
EasyExplorer/
├── src/                          # 前端源码
│   ├── index.html               # 主页面
│   ├── settings.html            # 设置页面
│   ├── renderer-tauri.js        # 主要 JS 逻辑
│   ├── css/                     # 编译后的 CSS
│   │   └── styles.css          # 主样式文件
│   ├── scss/                    # SCSS 源文件
│   │   ├── styles.sass         # 主样式入口
│   │   ├── _theme.sass         # 主题定义
│   │   └── ...
│   └── js/
│       └── settings-tauri.js    # 设置页面逻辑
│
├── src-tauri/                    # 后端源码
│   ├── src/
│   │   └── main.rs              # Rust 主文件
│   ├── Cargo.toml               # Rust 依赖配置
│   └── tauri.conf.json          # Tauri 配置
│
├── docs/                         # 文档
│   ├── 快速开始-转化版.md
│   ├── 热重载配置说明.md
│   ├── SCSS编译说明.md
│   └── ...
│
├── package.json                  # Node.js 配置
├── serve.js                      # HTTP 服务器
└── README-开发指南.md           # 本文件
```

---

## 💻 开发工作流

### 修改前端代码

```bash
# 1. 启动开发服务器
npm run tauri dev

# 2. 修改文件
编辑 src/renderer-tauri.js
编辑 src/scss/_theme.sass
编辑 src/index.html

# 3. 保存文件
Ctrl+S

# 4. 查看效果
应用自动刷新 ✨
```

### 修改后端代码

```bash
# 1. 启动开发服务器
npm run tauri dev

# 2. 修改 Rust 代码
编辑 src-tauri/src/main.rs

# 3. 保存文件
Ctrl+S

# 4. 等待编译
终端显示编译进度
应用自动重启 ✨
```

### 修改样式

```bash
# 1. 启动开发服务器
npm run tauri dev

# 2. 修改 SCSS 文件
编辑 src/scss/_theme.sass
编辑 src/scss/_view.sass

# 3. 保存文件
Ctrl+S

# 4. 自动编译
SCSS → CSS 自动编译
浏览器自动刷新 ✨
```

---

## 🎨 样式开发

### SCSS 文件组织

```
src/scss/
├── styles.sass              # 主入口
├── _theme.sass              # 主题（亮色/暗色）
├── _view.sass               # 视图样式
├── base/                    # 基础样式
├── components/              # 组件样式
└── ...
```

### 修改主题

**编辑 `src/scss/_theme.sass`**:
```sass
@mixin light-theme
  --bg-color: #ffffff
  --text-color: #000000

@mixin dark-theme
  --bg-color: #1e1e1e
  --text-color: #ffffff
```

保存后自动编译并刷新。

详见：[SCSS编译说明.md](./docs/SCSS编译说明.md)

---

## 🐛 调试

### 打开开发者工具

在应用中按 `F12` 或右键 → "检查"

### 查看日志

**前端日志**：
- 开发者工具 → Console 标签

**后端日志**：
- 查看终端输出
- Rust 的 `println!()` 会显示在终端

### 常见错误

#### 1. 端口 8080 被占用
```bash
# 查找占用进程
netstat -ano | findstr :8080

# 停止进程
taskkill /PID <进程ID> /F
```

#### 2. SCSS 编译错误
```bash
# 手动编译查看详细错误
npm run sass:build
```

#### 3. Rust 编译错误
```bash
# 进入 Rust 目录查看详细错误
cd src-tauri
cargo build
```

---

## 📚 文档索引

### 快速参考
- [快速开始](./docs/快速开始-转化版.md) - 基本使用
- [热重载配置](./docs/热重载配置说明.md) - 热重载详解
- [错误修复](./docs/错误修复说明.md) - 常见错误

### 功能说明
- [功能还原说明](./docs/功能还原说明.md) - 日历/年报/主题
- [鼠标事件说明](./docs/鼠标事件还原说明.md) - 滚轮/双击
- [函数转化报告](./docs/函数转化完成报告.md) - 实现统计

### 开发参考
- [SCSS 编译说明](./docs/SCSS编译说明.md) - 样式开发
- [函数迁移清单](./docs/函数迁移清单.md) - 所有函数对比
- [迁移实施方案](./docs/迁移实施方案.md) - 实施计划

### 测试
- [功能测试指南](./docs/功能测试指南.md) - 完整测试流程

### 总结
- [本次会话总结](./docs/本次会话总结.md) - 完整总结
- [本次更新总结](./docs/本次更新总结.md) - 更新汇总

---

## 🎯 最佳实践

### 1. 开发前
```bash
# 拉取最新代码
git pull

# 安装依赖（如果有新依赖）
npm install

# 启动开发服务器
npm run tauri dev
```

### 2. 开发中
- ✅ 频繁保存文件（自动重载）
- ✅ 使用开发者工具调试
- ✅ 查看控制台错误
- ✅ 使用 Git 频繁提交

### 3. 提交前
```bash
# 检查 linter 错误
npm run tauri dev
# 查看控制台是否有错误

# 测试基本功能
# 参考：docs/功能测试指南.md

# 提交代码
git add .
git commit -m "描述修改内容"
```

---

## 🔧 配置文件

### package.json
```json
{
  "scripts": {
    "tauri": "tauri",
    "serve": "node serve.js",
    "sass": "sass --watch src/scss:src/css",
    "sass:build": "sass src/scss:src/css",
    "dev": "concurrently \"npm run serve\" \"npm run sass\" \"tauri dev\"",
    "build": "npm run sass:build && tauri build"
  }
}
```

### tauri.conf.json
```json
{
  "build": {
    "beforeDevCommand": "npm run serve & npm run sass",
    "beforeBuildCommand": "npm run sass:build",
    "devPath": "http://localhost:8080"
  }
}
```

---

## 💡 技巧和窍门

### 1. 快速查看效果
```bash
# 启动后保持窗口在最前
# Windows 快捷键: Alt+T (在窗口中)
```

### 2. 多屏开发
- 一个屏幕：代码编辑器
- 另一个屏幕：应用窗口 + 终端

### 3. 使用 Source Map
- SCSS 编译自动生成 Source Map
- 在开发者工具中可以直接定位到 SASS 源文件

### 4. Git 工作流
```bash
# 创建功能分支
git checkout -b feature/new-feature

# 开发...

# 提交
git commit -m "feat: 添加新功能"

# 合并到主分支
git checkout main
git merge feature/new-feature
```

---

## 📞 获取帮助

### 查看文档
- 所有文档在 `docs/` 目录
- README 文件在项目根目录

### 常见问题
1. 查看 [错误修复说明.md](./docs/错误修复说明.md)
2. 查看 [功能测试指南.md](./docs/功能测试指南.md)
3. 查看控制台错误信息

### 调试步骤
1. 打开开发者工具 (F12)
2. 查看 Console 错误
3. 检查 Network 请求
4. 查看终端输出

---

## 🎉 现在开始开发

```bash
# 克隆项目（如果还没有）
git clone <repository-url>
cd EasyExplorer

# 安装依赖
npm install

# 启动开发
npm run tauri dev

# 开始编码 ✨
```

---

**祝开发愉快！** 🚀

如有问题，请查看 `docs/` 目录中的详细文档。

