# 📁 EasyExplorer 项目结构

## ✅ 整理后的目录结构

```
EasyExplorer/
├── src/                        # 前端源代码目录 ⭐ 新增
│   ├── index.html              # 主页面
│   ├── settings.html           # 设置页面
│   ├── renderer-tauri.js       # Tauri 版前端逻辑
│   ├── tauri-adapter.js        # Electron API 适配器
│   ├── css/                    # 编译后的样式文件
│   │   ├── styles.css
│   │   ├── settings.css
│   │   └── all.min.css
│   ├── scss/                   # Sass 源文件
│   │   ├── styles.sass
│   │   ├── _theme.sass
│   │   ├── _view.sass
│   │   └── ...
│   ├── js/                     # JavaScript 文件
│   │   ├── settings.js
│   │   └── occt-import-js.wasm
│   ├── assets/                 # 资源文件
│   │   └── icons/
│   └── webfonts/               # 字体文件
│       ├── fa-solid-900.ttf
│       └── ...
│
├── src-tauri/                  # Rust 后端代码
│   ├── src/
│   │   └── main.rs             # Rust 主程序
│   ├── icons/                  # 应用图标
│   ├── Cargo.toml              # Rust 依赖
│   ├── tauri.conf.json         # Tauri 配置
│   └── build.rs                # 构建脚本
│
├── docs/                       # 项目文档 ⭐ 新增
│   ├── README_TAURI.md         # Tauri 详细说明
│   ├── MIGRATION_NOTES.md      # 迁移技术文档
│   ├── 迁移完成总结.md          # 迁移总结
│   ├── REMOVED_DEPENDENCIES.md # 依赖变更清单
│   ├── 快速开始.md              # 入门指南
│   ├── 启动成功.md              # 启动说明
│   ├── 启动指南.md              # 启动指南
│   ├── 故障排除.md              # 故障排除
│   └── 重新启动说明.md          # 重启说明
│
├── node_modules/               # npm 依赖包
├── package.json                # npm 配置
├── package-lock.json           # npm 依赖锁定
├── serve.js                    # 开发服务器
├── icons.json                  # 图标配置
├── .gitignore                  # Git 忽略文件
├── 项目结构说明.md              # 本文件 ⭐ 新增
└── README.md                   # 项目主文档

```

## 📂 目录说明

### 1. `src/` - 前端源代码 ⭐ 核心目录

**包含所有前端相关文件**，整洁清晰：

- **HTML 文件** - 页面结构
- **JavaScript 文件** - 业务逻辑
- **CSS/SCSS 文件** - 样式
- **资源文件** - 图片、字体等

**优点**：
- ✅ 前端代码集中管理
- ✅ 易于维护和查找
- ✅ 构建时直接使用此目录

### 2. `src-tauri/` - Rust 后端

**Tauri 应用的后端代码**：

- `src/main.rs` - Rust 主程序，实现所有后端 API
- `Cargo.toml` - Rust 依赖配置
- `tauri.conf.json` - Tauri 应用配置
- `icons/` - 应用图标（各种尺寸）

### 3. `docs/` - 项目文档 ⭐ 新增

**所有项目文档集中在此**：

- 使用说明
- 技术文档
- 迁移指南
- 故障排除

**优点**：
- ✅ 文档不再散落在根目录
- ✅ 易于查找和维护
- ✅ 根目录更整洁

### 4. 根目录文件

只保留必要的配置文件：

- `README.md` - 项目主文档（必须在根目录）
- `package.json` - npm 配置
- `serve.js` - 开发服务器
- `.gitignore` - Git 配置
- `icons.json` - 图标配置

## 🔄 迁移前后对比

### 之前（根目录混乱）

```
EasyExplorer/
├── index.html
├── settings.html
├── renderer.js
├── renderer-tauri.js
├── tauri-adapter.js
├── README.md
├── README_TAURI.md
├── MIGRATION_NOTES.md
├── 迁移完成总结.md
├── 快速开始.md
├── 启动成功.md
├── 故障排除.md
├── ...（更多文档）
├── css/
├── scss/
├── js/
├── assets/
├── webfonts/
├── src-tauri/
└── ...
```

❌ 问题：
- 文件太多，难以找到
- HTML、JS、文档混在一起
- 不清楚哪些是源码，哪些是文档

### 之后（清晰有序）

```
EasyExplorer/
├── src/              # 👈 前端源码在这里
├── src-tauri/        # 👈 后端源码在这里
├── docs/             # 👈 文档在这里
├── package.json
├── serve.js
├── .gitignore
└── README.md
```

✅ 优点：
- 目录清晰，一目了然
- 按功能分类
- 易于导航和维护

## 🎯 文件查找指南

### 需要修改前端代码？
👉 到 `src/` 目录

### 需要修改后端代码？
👉 到 `src-tauri/src/` 目录

### 需要查看文档？
👉 到 `docs/` 目录

### 需要修改样式？
👉 到 `src/scss/` 目录（然后运行 `npm run sass`）

### 需要添加资源文件？
👉 到 `src/assets/` 目录

## 🔧 配置更新

已自动更新以下配置以适应新结构：

1. ✅ `src-tauri/tauri.conf.json`
   - `distDir: "../src"` - 指向 src 目录

2. ✅ `serve.js`
   - 从 `./src/` 目录提供文件

3. ✅ `package.json`
   - Sass 编译路径更新为 `src/scss:src/css`

## 🚀 使用方法

### 开发时

```bash
# 启动开发服务器（自动从 src 目录加载）
npm run dev

# 编译 Sass（监听 src/scss 变化）
npm run sass
```

### 构建时

```bash
# 构建生产版本（使用 src 目录）
npm run build
```

**所有命令照常使用，无需改变！**

## 📝 文件路径更新

### HTML 文件中的路径

无需修改！所有相对路径（如 `css/styles.css`）仍然有效，因为：
- HTML 文件在 `src/` 下
- CSS 文件在 `src/css/` 下
- 相对路径关系不变

### JavaScript 中的路径

同样无需修改，相对路径关系保持不变。

## 🎉 总结

通过这次整理：

- ✅ **根目录更整洁** - 只有 7-8 个文件/文件夹
- ✅ **结构更清晰** - 源码、文档、配置分离
- ✅ **易于维护** - 知道去哪里找什么
- ✅ **专业化** - 符合现代项目标准

---

**现在项目结构清晰多了！** 🎊

